# Backend API URL (for auth checks)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Dashboard App URL (for login/signup links)
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Google Tag Manager (optional — GA4 is configured as a tag inside GTM)
NEXT_PUBLIC_GTM_ID=

# Resend (newsletter subscription)
RESEND_API_KEY=
RESEND_AUDIENCE_ID=
